#!/bin/bash
# docker image cleanup
set -o pipefail
set -Ee
rm -f /tmp/.apt-update
apt-get autoremove -y -f
apt-get clean -y
rm -rf /var/lib/apt/lists/*
find /tmp/ /var/log/ -mindepth 1 -delete
rm -rf /root/.cache
